---
// About section component rediseñado
---

<section id="sobre-mi" class="section-padding bg-gradient-to-br from-gray-50 to-white relative overflow-hidden">
  <!-- Fondo decorativo -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-10 left-10 w-20 h-20 sm:w-32 sm:h-32 bg-yellow-400/10 rounded-full blur-xl"></div>
    <div class="absolute bottom-20 right-10 w-24 h-24 sm:w-40 sm:h-40 bg-red-600/10 rounded-full blur-xl"></div>
  </div>

  <div class="container-custom relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-8 sm:mb-12 md:mb-16 space-y-responsive">
      <div
        class="inline-flex items-center px-3 py-2 sm:px-4 sm:py-2 bg-yellow-400 border border-yellow-500 rounded-full text-yellow-900 text-xs sm:text-sm font-bold shadow-lg"
      >
        <svg class="w-3 h-3 sm:w-4 sm:h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
        </svg>
        Personal Trainer Certificado
      </div>
      <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold text-gray-900 text-balance">
        Conoce a <span class="bg-gradient-to-r from-yellow-400 to-red-600 bg-clip-text text-transparent">JeanFit</span>
      </h2>
      <p class="text-lg sm:text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Tu entrenador personal dedicado a transformar vidas a través del fitness y la nutrición.
      </p>
    </div>

    <!-- Main Content -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 sm:gap-12 lg:gap-16 xl:gap-20 items-center">
      <!-- Content Card -->
      <div class="space-y-6 sm:space-y-8 order-2 lg:order-1">
        <div class="space-y-4 sm:space-y-6">
          <div
            class="flex flex-col sm:flex-row sm:items-start gap-4 p-4 sm:p-6 bg-white rounded-xl shadow-soft smooth-transition hover:shadow-medium"
          >
            <div
              class="w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-br from-yellow-400 to-yellow-500 rounded-xl flex items-center justify-center flex-shrink-0"
            >
              <svg class="w-6 h-6 sm:w-7 sm:h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <div class="space-y-2">
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-gray-900">Experiencia Profesional</h3>
              <p class="text-sm sm:text-base md:text-lg text-gray-600 leading-relaxed">
                Más de 3 años ayudando a personas a alcanzar sus objetivos fitness con metodologías innovadoras y
                resultados comprobados.
              </p>
            </div>
          </div>

          <div
            class="flex flex-col sm:flex-row sm:items-start gap-4 p-4 sm:p-6 bg-white rounded-xl shadow-soft smooth-transition hover:shadow-medium"
          >
            <div
              class="w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-br from-red-500 to-red-600 rounded-xl flex items-center justify-center flex-shrink-0"
            >
              <svg class="w-6 h-6 sm:w-7 sm:h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
                ></path>
              </svg>
            </div>
            <div class="space-y-2">
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-gray-900">Educación Continua</h3>
              <p class="text-sm sm:text-base md:text-lg text-gray-600 leading-relaxed">
                Constantemente actualizando conocimientos en las últimas tendencias de fitness, nutrición y metodologías
                de entrenamiento.
              </p>
            </div>
          </div>

          <div
            class="flex flex-col sm:flex-row sm:items-start gap-4 p-4 sm:p-6 bg-white rounded-xl shadow-soft smooth-transition hover:shadow-medium"
          >
            <div
              class="w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center flex-shrink-0"
            >
              <svg class="w-6 h-6 sm:w-7 sm:h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"
                ></path>
              </svg>
            </div>
            <div class="space-y-2">
              <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-gray-900">Enfoque Personal</h3>
              <p class="text-sm sm:text-base md:text-lg text-gray-600 leading-relaxed">
                Cada plan está diseñado específicamente para ti, considerando tus objetivos, limitaciones y estilo de
                vida único.
              </p>
            </div>
          </div>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-2 sm:grid-cols-3 gap-4 sm:gap-6 pt-6 sm:pt-8 border-t border-gray-200">
          <div class="text-center">
            <div class="text-2xl sm:text-3xl md:text-4xl font-bold text-yellow-500 mb-1 sm:mb-2">500+</div>
            <div class="text-xs sm:text-sm md:text-base text-gray-600 leading-tight">
              Clientes<br class="sm:hidden" /> Transformados
            </div>
          </div>
          <div class="text-center">
            <div class="text-2xl sm:text-3xl md:text-4xl font-bold text-red-500 mb-1 sm:mb-2">95%</div>
            <div class="text-xs sm:text-sm md:text-base text-gray-600 leading-tight">
              Tasa de<br class="sm:hidden" /> Éxito
            </div>
          </div>
          <div class="text-center col-span-2 sm:col-span-1">
            <div class="text-2xl sm:text-3xl md:text-4xl font-bold text-blue-500 mb-1 sm:mb-2">3+</div>
            <div class="text-xs sm:text-sm md:text-base text-gray-600 leading-tight">
              Años de<br class="sm:hidden" /> Experiencia
            </div>
          </div>
        </div>
      </div>

      <!-- Image Card -->
      <div class="order-1 lg:order-2">
        <div class="relative">
          <!-- Background decorativo -->
          <div
            class="absolute inset-0 bg-gradient-to-br from-yellow-400/20 to-red-600/20 rounded-2xl sm:rounded-3xl transform rotate-3"
          >
          </div>
          <div class="relative bg-white rounded-2xl sm:rounded-3xl p-4 sm:p-6 md:p-8 shadow-large">
            <img
              src="/IMG_5758.jpeg"
              alt="JeanFit Personal Trainer"
              class="w-full h-auto responsive-image rounded-xl sm:rounded-2xl"
              style="aspect-ratio: 4/5; object-fit: cover;"
            />
            <!-- Badge flotante -->
            <div
              class="absolute -top-3 sm:-top-4 -right-3 sm:-right-4 bg-gradient-to-r from-yellow-400 to-red-600 text-white px-3 py-2 sm:px-4 sm:py-3 rounded-full font-bold text-xs sm:text-sm shadow-lg"
            >
              ✨ Certificado
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- CTA Section -->
    <div class="text-center mt-12 sm:mt-16 md:mt-20">
      <div class="bg-gradient-to-r from-yellow-400 to-red-600 rounded-2xl sm:rounded-3xl p-6 sm:p-8 md:p-12 text-white">
        <h3 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-3 sm:mb-4 md:mb-6">
          ¿Listo para comenzar tu transformación?
        </h3>
        <p class="text-base sm:text-lg md:text-xl mb-6 sm:mb-8 max-w-2xl mx-auto leading-relaxed">
          Únete a cientos de personas que ya han transformado su vida con métodos probados y seguimiento personalizado.
        </p>
        <a
          href="#contacto"
          class="inline-flex items-center justify-center bg-white text-yellow-600 font-bold text-sm sm:text-base md:text-lg px-6 py-3 sm:px-8 sm:py-4 rounded-full hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-lg"
        >
          ¡Comienza Ahora!
          <svg class="w-4 h-4 sm:w-5 sm:h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: 6rem 0;
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%, #f8fafc 100%);
    position: relative;
    overflow: hidden;
  }

  .about-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 20%, rgba(255, 214, 0, 0.03) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, rgba(237, 28, 36, 0.03) 0%, transparent 50%);
    pointer-events: none;
  }

  .container-custom {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 2;
  }

  /* Section Header */
  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-badge {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, rgba(255, 214, 0, 0.1) 0%, rgba(237, 28, 36, 0.1) 100%);
    border: 2px solid rgba(255, 214, 0, 0.2);
    border-radius: 50px;
    color: #1a1a1a;
    font-weight: 700;
    font-size: 0.9rem;
    margin-bottom: 2rem;
    backdrop-filter: blur(10px);
  }

  .section-title {
    font-size: 3rem;
    font-weight: 900;
    color: #1a1a1a;
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .section-title .highlight {
    background: linear-gradient(135deg, #ffd600 0%, #ed1c24 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-description {
    font-size: 1.25rem;
    color: #4a5568;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* About Grid */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    margin-bottom: 4rem;
  }

  .content-card {
    background: rgba(255, 255, 255, 0.95);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 2rem;
    padding: 3rem;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
    transform: translate3d(0, 0, 0);
    transition: transform 0.15s ease-out;
  }

  .content-card:hover {
    transform: translate3d(0, -4px, 0);
  }

  /* Experience Features */
  .experience-features {
    margin-bottom: 2.5rem;
  }

  .feature-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .feature-icon {
    width: 3rem;
    height: 3rem;
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: white;
  }

  .experience-icon {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
  }

  .education-icon {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  }

  .personalized-icon {
    background: linear-gradient(135deg, #f59e0b 0%, #dc2626 100%);
  }

  .feature-content h3 {
    font-size: 1.1rem;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 0.5rem;
  }

  .feature-content p {
    color: #4a5568;
    line-height: 1.6;
  }

  /* Specialties */
  .specialties-card {
    background: linear-gradient(135deg, rgba(255, 214, 0, 0.05) 0%, rgba(237, 28, 36, 0.05) 100%);
    border: 1px solid rgba(255, 214, 0, 0.2);
    border-radius: 1.5rem;
    padding: 2rem;
    margin-bottom: 2.5rem;
  }

  .specialties-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 1.5rem;
  }

  .specialties-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .specialty-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: transform 0.15s ease-out;
  }

  .specialty-item:hover {
    transform: translate3d(0, -2px, 0);
  }

  .specialty-icon {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .strength-icon {
    background: linear-gradient(135deg, #f59e0b 0%, #dc2626 100%);
  }

  .transformation-icon {
    background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
  }

  .nutrition-icon {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
  }

  .mindset-icon {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  }

  .specialty-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .specialty-title {
    font-weight: 600;
    color: #1a1a1a;
    font-size: 0.95rem;
  }

  .specialty-desc {
    font-size: 0.8rem;
    color: #6b7280;
  }

  /* CTA Buttons */
  .cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .primary-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #f59e0b 0%, #dc2626 100%);
    color: white;
    font-weight: 600;
    font-size: 1rem;
    padding: 1rem 2rem;
    border-radius: 50px;
    text-decoration: none;
    transition: all 0.15s ease-out;
    box-shadow: 0 6px 16px rgba(245, 158, 11, 0.25);
  }

  .primary-btn:hover {
    transform: translate3d(0, -2px, 0);
    background: linear-gradient(135deg, #fbbf24 0%, #ef4444 100%);
    box-shadow: 0 8px 20px rgba(245, 158, 11, 0.3);
  }

  .secondary-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(0, 0, 0, 0.1);
    color: #1a1a1a;
    font-weight: 600;
    font-size: 1rem;
    padding: 1rem 2rem;
    border-radius: 50px;
    text-decoration: none;
    transition: all 0.15s ease-out;
  }

  .secondary-btn:hover {
    transform: translate3d(0, -2px, 0);
    background: rgba(255, 255, 255, 1);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
  }

  /* Image and Stats Card */
  .image-stats-card {
    background: rgba(255, 255, 255, 0.95);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 2rem;
    padding: 2rem;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
    position: relative;
    transform: translate3d(0, 0, 0);
    transition: transform 0.15s ease-out;
  }

  .image-stats-card:hover {
    transform: translate3d(0, -4px, 0);
  }

  .profile-section {
    text-align: center;
    margin-bottom: 2rem;
  }

  .profile-image {
    position: relative;
    width: 200px;
    height: 200px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
  }

  .profile-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .image-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 1rem;
  }

  .availability-badge {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-weight: 700;
    font-size: 0.8rem;
    box-shadow: 0 4px 15px rgba(34, 197, 94, 0.4);
  }

  .profile-info h3 {
    font-size: 1.5rem;
    font-weight: 800;
    color: #1a1a1a;
    margin-bottom: 0.5rem;
  }

  .profile-info p {
    color: #4a5568;
    font-size: 1rem;
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .stat-card {
    background: linear-gradient(135deg, rgba(255, 214, 0, 0.05) 0%, rgba(237, 28, 36, 0.05) 100%);
    border: 1px solid rgba(255, 214, 0, 0.2);
    border-radius: 1rem;
    padding: 1.5rem 1rem;
    text-align: center;
    transition: transform 0.15s ease-out;
  }

  .stat-card:hover {
    transform: translate3d(0, -2px, 0);
  }

  .stat-number {
    font-size: 2rem;
    font-weight: 900;
    background: linear-gradient(135deg, #ffd600 0%, #ed1c24 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    font-size: 0.85rem;
    color: #4a5568;
    font-weight: 500;
  }

  /* Additional Info Cards */
  .info-cards {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .info-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: linear-gradient(135deg, rgba(255, 214, 0, 0.05) 0%, rgba(237, 28, 36, 0.05) 100%);
    border: 1px solid rgba(255, 214, 0, 0.15);
    border-radius: 1rem;
    transition: all 0.15s ease-out;
  }

  .info-card:hover {
    transform: translate3d(0, -2px, 0);
    border-color: rgba(255, 214, 0, 0.3);
    box-shadow: 0 4px 12px rgba(255, 214, 0, 0.1);
  }

  .info-icon {
    width: 2.5rem;
    height: 2.5rem;
    background: linear-gradient(135deg, #ffd600 0%, #ed1c24 100%);
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .info-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .info-title {
    font-weight: 600;
    color: #1a1a1a;
    font-size: 0.95rem;
  }

  .info-desc {
    font-size: 0.8rem;
    color: #6b7280;
  }

  /* Service Badge */
  .service-badge {
    text-align: center;
  }

  .service-badge span {
    background: linear-gradient(135deg, #1e1b3c 0%, #2a1f4f 100%);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 50px;
    font-weight: 700;
    font-size: 0.9rem;
    display: inline-block;
    box-shadow: 0 4px 15px rgba(30, 27, 60, 0.3);
  }

  /* Mission Card */
  .mission-card {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(255, 255, 255, 0.6) 100%);
    backdrop-filter: blur(20px);
    border: 2px solid rgba(255, 214, 0, 0.2);
    border-radius: 2rem;
    padding: 3rem 2rem;
    text-align: center;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
  }

  .mission-content h3 {
    font-size: 2.5rem;
    font-weight: 900;
    color: #1a1a1a;
    margin-bottom: 2rem;
  }

  .mission-content blockquote {
    font-size: 1.25rem;
    line-height: 1.7;
    color: #2d3748;
    font-style: italic;
    max-width: 800px;
    margin: 0 auto 2rem;
  }

  .mission-author {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #ffd600 0%, #ed1c24 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 4rem 0;
    }

    .container-custom {
      padding: 0 1rem;
    }

    .section-title {
      font-size: 2.5rem;
    }

    .content-card,
    .image-stats-card {
      padding: 2rem;
    }

    .specialties-grid {
      grid-template-columns: 1fr;
    }

    .stats-grid {
      grid-template-columns: 1fr 1fr;
    }

    .mission-card {
      padding: 2rem 1.5rem;
    }

    .mission-content h3 {
      font-size: 2rem;
    }

    .cta-buttons {
      flex-direction: column;
    }
  }
</style>

